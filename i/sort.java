public void insertionSort() {
  List<> sortL = new List<>();
  List<> unsortL = ;
  
  unsortL.toFirst();
  while (unsortL.isEmpty()) {
    cur = unsortL.getContent();
    
    boolean added = false;
    for (sortL.toFirst(); sortL.() && !added; sortL.next()) {
      if (sortL.getContent().getZeit()) {
        sortL.append(cur);
        added = true;
      }
    }
    
    unsortL.remove();
  }
}
